# Harness itself needs only the Python standard library.
# 'zstandard' lets the harness read .qasm.zst without the zstd CLI installed.
zstandard>=0.22

# --- Add whatever YOUR model needs below (examples) ---
# numpy
# scikit-learn
# joblib
